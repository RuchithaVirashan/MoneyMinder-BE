spring.application.name=moneyminder

# MySQL (Railway) datasource configuration
# Prefer setting MYSQL_JDBC_URL env var to a full JDBC URL, e.g.:
#   jdbc:mysql://ballast.proxy.rlwy.net:29691/railway?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
# Fallback to the explicit URL below if env var not set.
spring.datasource.url=${MYSQL_JDBC_URL:jdbc:mysql://ballast.proxy.rlwy.net:29691/railway?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${MYSQL_USERNAME:root}
spring.datasource.password=${MYSQL_PASSWORD:BQkwCIDrVaqjbGQAKFMazenSBYlZcecQ}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Connection pool (Hikari) optional tuning
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
